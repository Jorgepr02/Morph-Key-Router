import type { Plugin } from "@opencode-ai/plugin"

const plugin: Plugin = async () => {
  console.log("[morph-key-router] installed. Start manually with: morph-key-router web")

  return {}
}

export default plugin
